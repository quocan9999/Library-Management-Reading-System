import { BookDetailSkeleton } from '@/components/reader/book-detail/BookDetailSkeleton';

/** Skeleton loading cho trang chi tiết sách (/books/[slug]). */
export default function BookDetailLoading() {
  return <BookDetailSkeleton />;
}
